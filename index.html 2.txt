
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>الأسئلة الشائعة</title>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;600&display=swap');

  body {
    font-family: 'Cairo', sans-serif;
    background: #EEEEEE;
    color: #222;
    padding: 20px;
    max-width: 900px;
    margin: auto;
    text-align: center;
  }

  /* الأعلى: لوجو + العودة + اللغة */
  #top-bar {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: 15px;
    margin-bottom: 20px;
  }
  #top-bar img {
    height: 50px;
  }
  .back-btn {
    font-family: 'Cairo', sans-serif;
    display: inline-flex;
    align-items: center;
    background: #EEEEEE;
    color: #000000;
    padding: 10px 18px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 500;
    font-size: 1rem;
    transition: background 0.3s;
  }
  .back-btn:hover {
    background: #FF835E;
    color: #fff;
  }

  .lang-btn {
    padding: 10px 18px;
    border: none;
    border-radius: 8px;
    background-color: #FF835E;
    color: #fff;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
  }
  .lang-btn:hover {
    background-color: #e67350;
  }

  /* الأسئلة */
  #faq-container {
    background-color: #EEEEEE;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 0 8px rgba(0,0,0,0.1);
  }
  .accordion-item {
    background: #EEEEEE;
    margin-bottom: 15px;
    border-radius: 8px;
    overflow: hidden;
    text-align: right;
  }
  .accordion-header {
    background-color: #FF835E;
    color: white;
    cursor: pointer;
    padding: 18px 20px;
    font-size: 1.2rem;
    user-select: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: background-color 0.3s ease;
    border-radius: 8px;
  }
  .accordion-header:hover {
    background-color: #e67350;
  }
  .accordion-header::after {
    content: "\25BC";
    font-size: 1.2rem;
    transition: transform 0.3s ease;
    margin-left: 10px;
  }
  .accordion-header.active::after {
    transform: rotate(180deg);
  }
  .accordion-content {
    max-height: 0;
    overflow: hidden;
    background: #EEEEEE;
    padding: 0 20px;
    transition: max-height 0.4s ease, padding 0.4s ease;
    font-size: 1rem;
    line-height: 1.6;
    color: #333;
    border-radius: 0 0 8px 8px;
  }
  .accordion-header.active + .accordion-content {
    padding-top: 15px;
    padding-bottom: 15px;
  }
  .accordion-content p {
    margin: 10px 0;
    word-wrap: break-word;
  }
  @media (max-width: 480px) {
    body { padding: 10px; max-width: 100%; }
    .accordion-header { font-size: 1rem; padding: 15px 15px; }
    .accordion-content { font-size: 0.95rem; padding: 0 15px; }
    #top-bar { gap: 10px; flex-wrap: wrap; justify-content: center; }
  }
</style>
</head>
<body>

<div id="top-bar">
  <img src="https://i.postimg.cc/fT4NbGpp/image.png" alt="لوجو المتجر">
  <a href="https://klafstore.com" class="back-btn">العودة إلى الصفحة الرئيسة</a>
  <button id="lang-btn" class="lang-btn">EN</button>
</div>

<div id="faq-container"></div>

<script>
const faqs = {
  ar: [
    { question: "هل تواجه مشكلة عند تسجيل الدخول؟", answers: ["إذا كنت تواجه مشاكل في تسجيل الدخول، تأكد من صحة البريد الإلكتروني وكلمة المرور.", "حاول إعادة تعيين كلمة المرور إذا نسيتها."] },
    { question: "كيف أطلب منتج بالجملة؟", answers: ["لدعم طلبات الجملة، يمكنك تعبئة نموذج الطلب بالجملة الموجود في صفحة الطلبات.", "أو التواصل معنا عبر رقم الهاتف المخصص لطلبات الجملة."] },
    { question: "هل يمكنني إرجاع المنتج بعد الشراء؟", answers: ["نعم، يمكنك إرجاع المنتج خلال 14 يومًا من تاريخ الاستلام، بشرط أن يكون المنتج في حالته الأصلية وغير مستخدم.", "يرجى مراجعة سياسة الإرجاع المفصلة في صفحة الشروط والأحكام."] },
    { question: "كيف أتابع حالة طلبي؟", answers: ['يمكنك متابعة حالة طلبك من خلال الدخول إلى حسابك في الموقع ثم قسم "طلباتي".', "كما يمكن التواصل مع خدمة العملاء لأي استفسار عبر القنوات المتاحة."] }
  ],
  en: [
    { question: "Do you face any issues when logging in?", answers: ["If you are having trouble logging in, please check the correctness of your email and password.", "Try resetting your password if you forgot it."] },
    { question: "How do I order a product in bulk?", answers: ["To support bulk orders, you can fill out the bulk order form available on the orders page,", "or contact us via the dedicated phone number for bulk orders."] },
    { question: "Can I return the product after purchase?", answers: ["Yes, you can return the product within 14 days from the date of receipt, provided that the product is in its original condition and unused.", "Please review the detailed return policy on the terms and conditions page."] },
    { question: "How can I track my order status?", answers: ['You can track your order status by logging into your account and going to the "My Orders" section.', "You can also contact customer service for any inquiries through the available channels."] }
  ]
};

let currentLang = 'ar';

function renderFaq() {
  const container = document.getElementById('faq-container');
  container.innerHTML = '';
  faqs[currentLang].forEach(faq => {
    const item = document.createElement('div');
    item.className = 'accordion-item';
    const header = document.createElement('div');
    header.className = 'accordion-header';
    header.textContent = faq.question;
    const content = document.createElement('div');
    content.className = 'accordion-content';
    faq.answers.forEach(ans => {
      const p = document.createElement('p');
      p.textContent = ans;
      content.appendChild(p);
    });
    item.appendChild(header);
    item.appendChild(content);
    container.appendChild(item);
  });

  document.querySelectorAll('.accordion-header').forEach(header => {
    header.addEventListener('click', () => toggleAccordion(header));
    header.addEventListener('touchstart', e => { e.preventDefault(); toggleAccordion(header); });
  });
}

function toggleAccordion(header) {
  const activeHeader = document.querySelector('.accordion-header.active');
  if (activeHeader && activeHeader !== header) {
    activeHeader.classList.remove('active');
    activeHeader.nextElementSibling.style.maxHeight = null;
    activeHeader.nextElementSibling.style.paddingTop = '0';
    activeHeader.nextElementSibling.style.paddingBottom = '0';
  }
  header.classList.toggle('active');
  const content = header.nextElementSibling;
  if (header.classList.contains('active')) {
    content.style.maxHeight = content.scrollHeight + 'px';
    content.style.paddingTop = '15px';
    content.style.paddingBottom = '15px';
  } else {
    content.style.maxHeight = null;
    content.style.paddingTop = '0';
    content.style.paddingBottom = '0';
  }
}

// زر تغيير اللغة
document.getElementById('lang-btn').addEventListener('click', () => {
  currentLang = currentLang === 'ar' ? 'en' : 'ar';
  renderFaq();
  document.getElementById('lang-btn').textContent = currentLang === 'ar' ? 'EN' : 'عربي';
});

// العرض الأول
renderFaq();
</script>

</body>
</html>